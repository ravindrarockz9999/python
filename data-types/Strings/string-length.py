text = "Learn Python today, build the future tomorrow"
length = len(text)                         # Here len is keyword to find length of the string
print("The length of the string is:", length)

# we can use directly while printing
print("Length of string:", len(text)) 